using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Networking;


public class AudioSpawner : NetworkBehaviour {

    /*
    public static AudioSpawner instance;
    private List<string> spawnedAudioSources;

	// Use this for initialization
	void Start () {
		if(instance == null)
        {
            instance = this;
        }
	}
	
	[Command]
    public void CmdSpawnAudioSource(AudioClip clip, Vector3 position, string name)
    {
        if (!spawnedAudioSources.Contains(name)) // if the named audio source
        {

        }
    }
    */

}
